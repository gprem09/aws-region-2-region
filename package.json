{
  "name": "aws-region-2-region",
  "version": "0.1.0",
  "bin": {
    "aws-region-2-region": "bin/aws-region-2-region.js"
  },
  "scripts": {
    "build": "echo \"The build step is not required when using JavaScript!\" && exit 0",
    "cdk": "cdk",
    "test": "jest"
  },
  "devDependencies": {
    "aws-cdk": "2.160.0",
    "jest": "^29.7.0"
  },
  "dependencies": {
    "aws-cdk-lib": "2.160.0",
    "constructs": "^10.0.0"
  }
}
